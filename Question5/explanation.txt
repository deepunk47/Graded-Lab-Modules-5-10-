Command: cd ../Question5
Explanation:
This command navigates into the Question5 directory to keep all files related to this question organized.

Command: mkdir dirA dirB
Explanation:
This command creates two directories to be compared.

Command: touch dirA/file1.txt dirA/file2.txt dirA/common.txt
Explanation:
These commands create files in dirA, including a file common to both directories.

Command: touch dirB/file3.txt dirB/common.txt dirB/file4.txt
Explanation:
These commands create files in dirB, including a file common with dirA.

Command: touch compare_dirs.sh
Explanation:
This command creates an empty shell script file for directory comparison.

Command: chmod +x compare_dirs.sh
Explanation:
This command grants execute permission to the directory comparison script.

Command: nano compare_dirs.sh
Explanation:
This command opens the script file in the nano editor to write logic using ls, sort, and comm.

Command: ./compare_dirs.sh dirA dirB
Explanation:
This command executes the script and displays common and unique files between the two directories.

Command: ./compare_dirs.sh dirA
Explanation:
This command tests the script with insufficient arguments.

Command: ./compare_dirs.sh dirA wrongdir
Explanation:
This command tests the script with a non-existent directory.

Command: git add .
Explanation:
This command stages all Question5 files for committing.

Command: git commit -m "Question 5 completed"
Explanation:
This command commits the Question 5 files with a descriptive message.

Command: git push
Explanation:
This command pushes the committed changes to the remote repository.
